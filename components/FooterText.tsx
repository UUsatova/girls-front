'use client'

export default function FooterText() {
  return (
    <div className="pointer-events-none fixed inset-x-0 bottom-4 flex justify-center px-4 sm:px-0">
      <p className="text-center text-[0.75rem] sm:text-xs text-gray-medium">
        18+ only. We don't store your chats unnecessarily.
      </p>
    </div>
  )
}

